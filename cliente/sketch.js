function setup() {
  createCanvas(800, 600);
  background(30);
  textAlign(CENTER, CENTER);
  textSize(24);
  fill(255);
  text("Preparando la Batalla de Ruido...", width / 2, height / 2);
}

function draw() {
  // Aquí irá la lógica visual y de sonido más adelante
}
